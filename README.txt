1. Как делать коментарии в листинге
    .1. ; до конца строки - точка с запятой

    .2. ;;;; Традиционно комментарии разного уровня отличаются количеством точек с запятой - здесь начало "главы" в файле
        ;;; комментарий на верхнем уровне - к одному определению

    .3. ;; комментарий к участку кода внутри функции
        (cond ((null p1)             ; в конце строки - к оператору

    .4. #| блочный #|

    .5. с поддержкой вложенности
        |#  решётка палка в начале и 
            палка решётка в конце |#

    .6. (defvar *grammar* nil "Активная грамматика.")
        (deftype index ()
          "Тип номеров элементов массива."
          `(integer 0 (,most-positive-fixnum)))

2. трассировка
CL-USER 34 : 4 > trace myappend
(MYAPPEND)

CL-USER 35 : 4 > myappend `(1 2) `()
0 MYAPPEND > ...